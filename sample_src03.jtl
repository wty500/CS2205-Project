template<typename T, typename U>
T fun(T a, U b) {
  if(a==0||b==0)then{return}
  else{
    a=fun(a,b-1);
    b=fun(b,a-1)
  }
};

void main(void(*a)(), int*(**b)(int*,int*)){
  var int a;
  a=1;
  var int b;
  b=2;
  var int c;
  c=fun(a,b)
}