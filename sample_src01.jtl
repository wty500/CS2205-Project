template<typename T, typename U>
void fun(T x, U y){
  if(x==0) then {return} else {fun(y-1, x+1)}
};

void main(int** a, int*** b) {
  var int n;
  n = 0;
  var int **m;
  **m = n + 1;
  fun(**m + 2, m)
}
