var int*(**n)(int(*)(int, int**), int**);

var void(**p)(int, int**);

var void(**p)();

int(*)(int, int**) main(int** a, int*** b) {
  var int(**n)(int, int**);
  var int **m;
  **m = ***n + 1;
  write_int(**m + 2)
}
